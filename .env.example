# Root-level example environment for local development and Docker Compose

# Backend
BACKEND_PORT=3001
BACKEND_NODE_ENV=production
JWT_SECRET=supersecretkey_example

# PostgreSQL database
POSTGRES_DB=conduit
POSTGRES_USER=conduit
POSTGRES_PASSWORD=conduit

# Frontend (Vite)
# Used at build time for the frontend. Leave empty to use same-origin
# requests (recommended for docker-compose with nginx proxy).
VITE_API_BASE_URL=
